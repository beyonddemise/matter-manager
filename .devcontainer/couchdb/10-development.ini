; Development overlay. Everything else is in infra/couchdb/00-base.ini — including the
; authentication settings, so that sign-in and replication behave here as they do in
; production rather than only in production.

[cors]
; Vite's dev server and `vite preview`.
origins = http://localhost:5173, http://127.0.0.1:5173, http://localhost:4173
