{
  "name": "@matter-manager/e2e",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "End-to-end journeys against the built site. Belongs to neither half: it drives the frontend through a browser and exercises the service worker, the shipped bundle and a real IndexedDB.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  }
}
